<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.notes1">

    <!-- Runtime permission example: READ_EXTERNAL_STORAGE for attaching images -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Notes1">

        <activity
            android:name="com.example.notes1.MainActivity"
            android:exported="true"



            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.example.notes1.AddNoteActivity"
            android:label="@string/add_note"
            android:parentActivityName="com.example.notes1.MainActivity" />

        <activity
            android:name="com.example.notes1.NoteDetailActivity"
            android:label="@string/note_detail"
            android:parentActivityName="com.example.notes1.MainActivity" />

    </application>
</manifest>
